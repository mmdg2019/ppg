<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View -->
    <record id="view_stock_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.car.number</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='location_id'][2]" position="after">
                <field name="car_number_id" widget="many2one" options="{'no_open': True, 'no_create': True}"/>
            </xpath>
             <!-- <xpath expr="//group" position="after">
                <group name="car_info_group" string="Car Information">
                    <group>
                        <field name="car_number_id" widget="many2one" />
                        <field name="car_size" readonly="1" />
                        <field name="car_ton" readonly="1" />
                    </group>
                    <group>
                        <field name="car_length" readonly="1" />
                        <field name="car_width" readonly="1" />
                        <field name="car_height" readonly="1" />
                    </group>
                </group>
            </xpath> -->
        </field>
    </record>
</odoo>